import styled from "styled-components";

const HeaderStyle = styled.header`
  background: linear-gradient(310deg, rgb(1, 63, 110), rgb(0, 114, 187));
  width: 100vw;
  font-size: 30px;
`;

export default HeaderStyle;
